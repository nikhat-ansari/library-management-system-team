export interface Category {
  _id: string;
  name: string;
  createdAt?: string;
  updatedAt?: string;
}

export interface Author {
  _id: string;
  name: string;
  createdAt?: string;
  updatedAt?: string;
}

export interface Publisher {
  _id: string;
  name: string;
  createdAt?: string;
  updatedAt?: string;
}
