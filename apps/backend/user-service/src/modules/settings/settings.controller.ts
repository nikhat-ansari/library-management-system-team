import { Body, Controller, Delete, Get, Param, Post, Put } from '@nestjs/common';
import { SettingsService } from './settings.service'; import { UpdateSystemSettingsDto } from './dto/settings.dto'; import { CreateHolidayDto } from './dto/holiday.dto';
@Controller('admin') export class SettingsController { constructor(private readonly service: SettingsService) {} @Get('settings') getSettings() { return this.service.getSettings(); } @Put('settings') update(@Body() dto: UpdateSystemSettingsDto) { return this.service.updateSettings(dto); } @Get('holidays') holidays() { return this.service.listHolidays(); } @Post('holidays') add(@Body() dto: CreateHolidayDto) { return this.service.addHoliday(dto); } @Delete('holidays/:id') remove(@Param('id') id: string) { return this.service.removeHoliday(id); } }
